<h1>Listing micrpposts</h1>

<table>
  <tr>
    <th>Content</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @micrpposts.each do |micrppost| %>
  <tr>
    <td><%= micrppost.content %></td>
    <td><%= micrppost.user_id %></td>
    <td><%= link_to 'Show', micrppost %></td>
    <td><%= link_to 'Edit', edit_micrppost_path(micrppost) %></td>
    <td><%= link_to 'Destroy', micrppost, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Micrppost', new_micrppost_path %>
